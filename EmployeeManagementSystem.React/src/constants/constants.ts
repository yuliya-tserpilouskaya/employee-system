// src/constants/constants.ts

export enum Sex {
    Male = 'Male',
    Female = 'Female',
    Other = 'Other',
    PreferNotToSay = 'Prefer not to say'
}

// Optional: If you have age constants, you could add them here too
export const AgeConstants = {
    MinAge: 18,
    MaxAge: 100,
};
